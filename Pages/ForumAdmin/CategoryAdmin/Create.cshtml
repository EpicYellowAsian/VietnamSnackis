@page
@model VietnamSnackis.Pages.CategoryAdmin.CreateModel

@{
    ViewData["Title"] = "LÄGG TILL NY KATEGORI";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>LÄGG TILL NY KATEGORI</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Category.Name" class="control-label">Namn</label>
                <input asp-for="Category.Name" class="form-control" />
                <span asp-validation-for="Category.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Skapa Kategori" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Tillbaka</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
